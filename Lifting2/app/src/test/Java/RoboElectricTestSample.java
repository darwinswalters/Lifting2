import com.example.walter16.lifting2.BuildConfig;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricGradleTestRunner;
import org.robolectric.annotation.Config;

/**
 * Created by darwinwalters on 8/14/15.
 */
@RunWith(RobolectricGradleTestRunner.class)
@Config(constants = BuildConfig.class)
    class userSelectActivityTest {

    @Test
    public void testLoadMenuFromUserList() {

        //SelectUserActivity selectUserActivity = Roboelectric.setupActivity(SelectUserActivity.class);

        //test should bring up Activity that allows user to log workouts or
        //calculate the cycle for a new 1RM

    }
}
